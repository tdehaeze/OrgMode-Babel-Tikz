\documentclass[10pt,tikz]{standalone}

\ifstandalone%
\usepackage{import}
\import{../../configuration/}{comon_packages.tex}%
\import{../../configuration/}{variables.tex}%
\import{../../configuration/}{conftikz.tex}%
\import{../../configuration/}{custom_config.tex}%
\fi

\begin{document}
\begin{tikzpicture}
  % Parameters
  \def\spacing{1.0}
  \def\blockw{1.2cm}
  \def\blockh{1.0cm}
  \def\addsize{0.7cm}

  % Blocks
  \node[draw, DAC] (dac) {DAC};
  \node[draw, block, right=\spacing of dac, label={[align=center]below:Current\\Injector}] (ci) {$H_{\text{CI}}$};
  \node[draw, block, right=\spacing of ci, label={[align=center]below:Voice\\Coil}] (vc) {$H_{\text{VC}}$};
  \node[draw, block, right=\spacing of vc] (G) {$G$};
  \node[draw, add, right=\spacing of G] (addy) {};
  \node[draw, block, above=\spacing of addy] (Gd) {$G_D$};
  \node[draw, block, right=\spacing of addy, label={[align=center]below:Interferometer}] (int) {};
  \node[draw, ADC, right=\spacing of int] (adc) {ADC};
  \coordinate[] (adc_output) at ($(adc.east) + (0.8*\spacing, 0)$);
  \coordinate[] (dac_input) at ($(dac.west) + (-0.8*\spacing, 0)$);
  \node[draw, block, below=\spacing of G, label={[align=center]below:Controller}] (K) {$K$};

  % Connections and labels
  \draw[->] (adc.east) -- (adc_output) |- (K.east);
  \draw[->] (K.west) -| (dac_input) -- (dac.west);
  \draw[->] (dac.east) -- node[midway, above]{$V$} (ci.west);
  \draw[->] (ci.east) -- node[midway, above]{$I$} (vc.west);
  \draw[->] (vc.east) -- node[midway, above]{$F$} (G.west);
  \draw[->] (G.east) -- node[midway, above]{} (addy.west);
  \draw[->] (addy.east) -- node[midway, above]{$y[m]$} (int.west);
  \draw[->] (int.east) -- node[midway, above]{$y_m$} (adc.west);
  \draw[->] (Gd.south) -- node[midway, right]{} (addy.north);
  \draw[<-] (Gd.north) -- ++(0, 0.8*\spacing) node[right]{$d$};
  \draw[<-] (ci.north) -- ++(0, 0.8*\spacing) node[right]{$n_{\text{CI}}$};
  \draw[<-] (int.north) -- ++(0, 0.8*\spacing) node[right]{$n$};
\end{tikzpicture}
\end{document}

